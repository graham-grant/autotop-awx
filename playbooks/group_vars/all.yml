# Credentials are pulled from environment variables rather than committed
# here. Before running the playbook:
#   export NAPALM_USERNAME=admin
#   export NAPALM_PASSWORD=secret
napalm_username: "{{ lookup('env', 'NAPALM_USERNAME') }}"
napalm_password: "{{ lookup('env', 'NAPALM_PASSWORD') }}"
